{
  "private": true,
  "name": "http-lambda",
  "description": "HTTP Lambda. Write AWS Lambdas the same way as with a local server.",
  "version": "0.0.10",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=6.10"
  },
  "peerDependencies": {
    "lodash": "^4.0.0"
  },
  "scripts": {}
}
